# Assessment Requirement Traceability

This checklist maps each required item from the Valsoft brief to code and evidence in this repository.

## Section 3.1 - Required Workflow Steps

| Requirement | Implementation | Evidence Artifact |
|---|---|---|
| Step 1: Ingestion auto-start on new message | `ingestion/webhook_api.py` (`POST /intake`) with validation, rate limiting, and optional API-key auth | `docs/demo_runbook.md` webhook demo + auth variants |
| Step 2: Classification (category, priority, confidence) | `workflow/nodes.py` -> `classify_node` with strict guardrail validation and explicit classification errors | `output/submission_records.json` (`category`, `priority`, `confidence`, `confidence_level`, `confidence_source`, `classification_guardrail_flags`) |
| Step 3: Enrichment (core issue, identifiers, urgency) | `workflow/nodes.py` -> `enrich_node` | `output/submission_records.json` |
| Step 4: Routing to destination queue + fallback | `workflow/nodes.py` -> `route_node` with keyword, billing delta, and one-amount dispute escalation | `proposed_queue`, `destination_queue`, `escalation_rules_triggered`, `escalation_rule_evidence` |
| Step 5: Structured JSON output + human summary | `workflow/nodes.py` -> `output_node` (append-safe JSONL + metadata); `scripts/generate_submission_artifacts.py` | `output/processed_records.jsonl`, `output/submission_records.json`, `output/submission_summary.md` |
| Step 6: Human escalation when confidence < 70% or escalation criteria | `workflow/nodes.py` escalation rules + thresholds in `config/settings.py` | `escalation_flag`, `destination_queue=Human Review`, reason + evidence fields |

## Section 4 - Deliverables

| Deliverable | Implementation / Location |
|---|---|
| 4.1 Working workflow | `app.py` (UI), `main.py` (CLI), `ingestion/webhook_api.py` (auto-ingestion path) |
| 4.2 Structured output file | `output/submission_records.json` (generated by `scripts/generate_submission_artifacts.py`) |
| 4.3 Prompt documentation | `prompts/prompt_documentation.md` |
| 4.4 Architecture write-up | `docs/architecture_writeup.md` |

## Before vs After (Input-First Boost)

| Area | Before | After |
|---|---|---|
| Intake contract | `source`, `message`, optional `external_id` | Added `request_id`, `customer_id`, `received_at`, `channel_metadata` with validation |
| Intake security | Open endpoint only | Optional `X-API-Key` enforcement |
| Idempotency | In-memory dedup set | Persistent SQLite idempotency keyed by request/content hash |
| Runtime persistence | Read-modify-write JSON array | Append-safe JSONL (`output/processed_records.jsonl`) |
| Escalation billing coverage | Delta rule (2+ amounts) only | Delta rule + one-amount dispute language rule |
| Observability | Ad-hoc prints | Structured log events + `ingestion_id`, `processing_ms`, `pipeline_version` |

## Evaluation Criteria Coverage

| Dimension | Repo Evidence |
|---|---|
| Workflow Functionality | End-to-end graph in `workflow/graph.py`; webhook/UI/CLI entrypoints; persistent replay protection |
| Classification & Prompt Quality | Few-shot prompts + output guardrails + surfaced guardrail flags |
| System Design Thinking | Routing/escalation logic, idempotency design, append-safe persistence, optional auth, trace metadata |
| Structured Output Quality | Rich schema with classification/enrichment/routing/escalation/telemetry fields |
| Documentation & Communication | README, architecture write-up, prompt docs, runbook, this traceability matrix |
